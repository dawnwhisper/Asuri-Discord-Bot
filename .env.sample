# Sample .env file
APP_ID=
PUBLIC_KEY=
DISCORD_TOKEN=
# Add your status monitoring URL here (optional)
STATUS_URL=

# --- LLM Configuration ---
# Choose ONE provider and set its API key and optionally model
# LLM_PROVIDER=siliconflow # or openai, gemini, etc. (Defaults to siliconflow if not set)

# SiliconFlow Settings (if LLM_PROVIDER is 'siliconflow' or default)
SILICONFLOW_API_KEY=your_siliconflow_api_key_here
# SILICONFLOW_MODEL=Qwen/Qwen2-7B-Instruct # Optional, defaults in config
# SILICONFLOW_ENDPOINT=https://api.siliconflow.cn/v1/chat/completions # Optional, defaults in config

# OpenAI Settings (if LLM_PROVIDER is 'openai')
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL=gpt-4 # Optional, defaults to gpt-3.5-turbo in config

# Google Gemini Settings (if LLM_PROVIDER is 'gemini')
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-pro # Optional, defaults to gemini-pro in config

# Custom LLM Example (if LLM_PROVIDER is 'custom')
# CUSTOM_LLM_ENDPOINT=your_custom_llm_api_endpoint
# CUSTOM_LLM_API_KEY=your_custom_llm_api_key
